- content_for :breadcrumbs do
  =link_to 'Apps', apps_path, class: 'breadcrumb-item'
  %span.breadcrumb-item.active Create an app

- content_for :toolbar do
  = link_to '← Return to apps', apps_path, class: 'btn btn-secondary'

.container
  = render 'form'
